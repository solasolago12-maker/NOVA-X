# NOVA-X
NOVA-X is a lightweight terminal-based AI homework assistant that supports Gemini, Ollama, and OpenAI providers. It includes robust Ollama host/model validation so model IDs like qwen2.5:7b are never mistaken for a server URL, plus a TUI, provider commands, and CI-backed tests.
